# Description: Library for manipulation of TIFF (Tag Image File Format) images
# URL:         http://www.remotesensing.org/libtiff/
# Maintainer:  Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on:  libtiff, libjpeg-compat32, zlib-compat32

name=libtiff-compat32
version=3.8.2
release=2
source=(ftp://ftp.remotesensing.org/libtiff/tiff-$version.tar.gz \
        libtiff-$version.patch)

build() {
    cd tiff-$version
    patch -p1 -i $SRC/libtiff-$version.patch
    ./configure --prefix=/usr \
	--libdir=/usr/lib32 \
	--with-zlib-lib-dir=/usr/lib32 \
	--with-jpeg-lib-dir=/usr/lib32
    
    make
    make DESTDIR=$PKG install
    rm -rf $PKG/usr/{bin,share,man,include}
}
