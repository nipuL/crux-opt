# Description: core database of common types
# URL: http://freedesktop.org/Software/shared-mime-info
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Depends on: glib,libxml2,p5-xml-parser
# Group: gnome

name=shared-mime-info
version=0.20
release=1
source=(http://freedesktop.org/~hadess/$name-$version.tar.bz2 \
        $name.patch)

build() {
	cd $name-$version
	patch -p1 < ../$name.patch
	./configure --prefix=/usr
	make
	make DESTDIR=$PKG install

	install -d -m 755 $PKG/usr/lib/pkgconfig
	mv $PKG/usr/share/pkgconfig/shared-mime-info.pc $PKG/usr/lib/pkgconfig
	rmdir $PKG/usr/share/pkgconfig
}
