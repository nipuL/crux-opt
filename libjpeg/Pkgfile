# Description: Library of JPEG support functions.
# URL:         http://www.ijg.org/
# Maintainer:  Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Packager:  Hannes Mayer, crux86_64 at ecarux dot de

name=libjpeg
version=6b
release=1
source=(ftp://ftp.uu.net/graphics/jpeg/jpegsrc.v$version.tar.gz)

build() {
    cd jpeg-$version
    ./configure --prefix=/usr --enable-shared --enable-static
    sed -i 's/\.\/libtool/libtool/' Makefile
    make
    mkdir -p $PKG/usr/include $PKG/usr/lib
    make prefix=/$PKG/usr install-lib
}
