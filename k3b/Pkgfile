# Description: a CD Burning tool for KDE3
# URL: http://k3b.plainblack.com
# Maintainer:
# Packager: 
# Depends on: kdelibs, cdrtools, cdparanoia, cdrdao, taglib, flac, normalize, dvd+rw-tools, lame, libsndfile, ffmpeg, libmusicbrainz, libsamplerate

name=k3b
version=0.12.16
release=1
source=(http://dl.sourceforge.net/sourceforge/k3b/$name-$version.tar.bz2)

build() 
{
    cd $name-$version
    ./configure --prefix=/usr \
        --disable-debug
    make
    make DESTDIR=$PKG install
    find $PKG/usr/share/doc/HTML/* ! -wholename "*HTML/en*" -delete
}
