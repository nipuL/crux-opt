#
# /etc/my.cnf
#

[mysqld]
datadir  = /var/lib/mysql
user     = daemon
log      = /var/log/mysqld.log
pid-file = /var/run/mysqld.pid
socket   = /tmp/mysql.sock
port     = 3306

skip-networking

[client]
socket   = /tmp/mysql.sock
port     = 3306

# End of file
