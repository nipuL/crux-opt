# Description: command line application for maintaining lists of tasks
# URL: http://swapoff.org/DevTodo
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au

name=devtodo
version=0.1.20
release=2
source=(http://swapoff.org/files/$name/$name-$version.tar.gz
    $name-gcc-include.patch)

build () {
	cd $name-$version

	patch -p1 -i $SRC/$name-gcc-include.patch

	./configure --prefix=/usr --mandir=/usr/man

	make
	make DESTDIR=$PKG install
}
