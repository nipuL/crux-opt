# Description: Renames multiple files using regular expressions
# URL: http://search.cpan.org/~pederst/rename-1.4/
# Maintainer: Jukka Heino, jukka at karsikkopuu dot net
# Packager: Jukka Heino, jukka at karsikkopuu dot net
# Depends on:

name=p5-rename
version=1.4
release=1
source=(http://search.cpan.org/CPAN/authors/id/P/PE/PEDERST/rename-$version.tar.gz)

build() {
    cd rename-$version
    perl Makefile.PL
    make
    make DESTDIR=$PKG install
    mv $PKG/usr/bin/rename $PKG/usr/bin/p5-rename
    mv $PKG/usr/man/man1/rename.1pm $PKG/usr/man/man1/p5-rename.1pm
    rm -rf $PKG/usr/lib
}
