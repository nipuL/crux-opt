# Description: reads your hdd temperature using S.M.A.R.T values
# URL: http://www.guzu.net/linux/hddtemp.php
# Maintainer:
# Packager: 
# Depends on: gettext

name=hddtemp
version=0.3-beta14
release=1
source=(http://www.guzu.net/linux/$name-$version.tar.gz)

build() {
    cd $name-$version
    ./configure --with-db-path=/usr/etc/hddtemp.db \
        --prefix=/usr
    make
    make DESTDIR=$PKG install
    mkdir -p $PKG/usr/etc
    wget -O $PKG/usr/etc/$name.db http://www.guzu.net/linux/$name.db	
    rm -rf $PKG/usr/share
}
