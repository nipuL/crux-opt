# Description: a Mplayer & Xine frontend for KDE; it has a kpart for konqueror integration
# URL: http://kmplayer.kde.org/
# Maintainer:
# Packager: 
# Depends on: kdelibs, mplayer

name=kmplayer
version=0.9.2
release=1
source=(http://kmplayer.kde.org/pkgs/kmplayer-$version.tar.bz2)

build() {
    cd $name-$version
    ./configure --prefix=/usr \
        --without-gstreamer
    make
    make DESTDIR=$PKG install
    find $PKG/usr/share/{doc/HTML,locale}/* ! -wholename "*HTML/en*" \
        ! -wholename "*locale/en*" -delete
}

