# Description: Vorbis codec library
# URL:         http://www.xiph.org/ogg/vorbis/
# Maintainer:  Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Depends on:  libvorbis libogg-compat32

name=libvorbis-compat32
version=1.2.3
release=1
source=(http://downloads.xiph.org/releases/vorbis/libvorbis-$version.tar.bz2)

build() {
    cd libvorbis-$version
    ./configure --prefix=/usr \
	--libdir=/usr/lib32
    make
    make DESTDIR=$PKG install
    rm -rf $PKG/usr/{include,share}
}
